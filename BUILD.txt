ORDO / ALTYN KHAN 3D build: 0.5.5
Tuning panel follow-up per user feedback (panel was covering the field,
making it impossible to see the result of a slider while dragging it):
- #tunePanel background made mostly transparent (~0.2 alpha) so the 3D
  field/carpet stays visible underneath; text/controls keep a strong
  text-shadow for contrast so the panel stays readable over any background.
- Added position sliders (X/Y) for the game title (uiTitleX/uiTitleY,
  translating .game-title-inline) and the wallet plate (uiWalletX/uiWalletY,
  translating .wallet) - previously only their scale was tunable, not their
  position.
All new offsets default to 0px (no visual change until moved). 43 unique
[data-tune] keys total now (was 39 in v0.5.4).
