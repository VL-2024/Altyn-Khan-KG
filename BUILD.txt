ORDO / ALTYN KHAN 3D build: 0.5.6
Baked the user's live-tuned values (from "Копия" JSON on the tuning panel,
tested on a real host where the Babylon CDN is reachable) into
TUNE_DEFAULTS in src/game.js, replacing the placeholder v0.5.4 defaults.
Every one of the 43 tuning keys now ships as the tuned look out of the box
for a fresh player (no localStorage) - carpet/pile/camera/model placement
plus the 2D UI overlay (logo/title/wallet position+scale, hint/score/
bottom/action font+plate scale). No code changes beyond the default
values themselves; the tuning panel and its slider ranges are unchanged.
