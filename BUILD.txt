ORDO / ALTYN KHAN 3D build: 0.5.10
Preloader background changed from a two-stop gradient (#0c2c56 -> #02142f)
to a flat #082b5a, per user request - the artwork's own background is
flat, so a gradient could only ever match it at one screen position. Flat
means an exact, guaranteed match everywhere, with the mask (v0.5.9) now
having nothing to seam against.
Verified via Playwright screenshot: no visible edge/rectangle around the
logo at all.
