ORDO / ALTYN KHAN 3D build: 0.5.3
LMS integration synced with sibling X2 LOTO games (Mahjong Luck / Upay / ЧҮКӨ-ОРДО):
sessionMode postMessage -> cookie, scenario response parsed as [id, unused]
array, Method=Balance keep-alive polling wired up (was defined in
lms-adapter.js but never called), Method=Balance request/response shape
corrected to match the real backend contract (single Method=-based
endpoint, idIG/idSK, Accounts/Balans response shape) instead of the old
placeholder REST-style /api/lms/player/balance path.
Visual/physics/ballistics unchanged from v0.5.2 (KHAN visual polish).
