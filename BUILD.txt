ORDO / ALTYN KHAN 3D build: 0.5.8
Swapped the preloader's placeholder inline "X2 LOTO" text-SVG logo for the
user-supplied branded artwork (assets/preloader-logo.webp - mascot + X2
LOTO wordmark), sized responsively (min(220px,58vw)) with a rounded corner
so its own dark background blends into the preloader's navy gradient.
No other preloader behavior changed (v0.5.7's stage-driven progress bar,
app-ready/app-fallback classes, etc. are untouched).
